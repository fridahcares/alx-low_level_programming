#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: character being multiplied
 * @b: character being multiplied by
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
