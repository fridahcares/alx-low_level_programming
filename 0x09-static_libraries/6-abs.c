#include "main.h"
/**
 * _abs - omputes the absolute value of an integer
 * @i: character being checked
 *
 * Return: absolute number
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
